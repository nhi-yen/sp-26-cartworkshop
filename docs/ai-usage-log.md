<h1>AI Usage Log – Cart Labs</h1>

<h2>Session: Cart UI Workshop</h2>
<hr/>

<h3>AGENTS.md Setup</h3>
<p><strong>What:</strong> Created AGENTS.md with real project conventions (component folder structure, CSS Modules, reducer patterns, strict TypeScript, import paths).<br/>
<strong>Result:</strong> AI-generated components matched my codebase structure, used correct import paths, and followed my CSS Module conventions.</p>

<hr/>

<h3>Cart Reducer + Types (Part 2)</h3>
<p><strong>Generated by:</strong> GitHub Copilot (VS Code agent)</p>
<p><strong>Review issues found:</strong></p>
<ul>
  <li>AI initially used non-type imports (<code>import { CartAction }</code>) which broke under <code>verbatimModuleSyntax</code>.</li>
  <li>Fixed by converting all type imports to <code>import type { … }</code>.</li>
  <li>Ensured action shapes matched the discriminated union exactly.</li>
</ul>
<p><strong>Concepts verified:</strong></p>
<ul>
  <li>Discriminated unions for CartAction.</li>
  <li>Pure reducer (no side effects, no mutations).</li>
  <li>Immutable updates using spreads (<code>...state</code>, <code>...item</code>).</li>
  <li>Duplicate add logic: <code>ADD_TO_CART</code> increments quantity if item exists.</li>
  <li>No default case to allow TypeScript exhaustive checking.</li>
</ul>

<hr/>

<h3>Cart Context (Part 3)</h3>
<p><strong>Generated by:</strong> GitHub Copilot</p>
<p><strong>Modifications I made:</strong></p>
<ul>
  <li>Ensured imports used <code>import type</code> for CartState and CartAction.</li>
  <li>Verified derived values (<code>cartItemCount</code>, <code>cartTotal</code>) were computed inside the provider, not stored in state.</li>
  <li>Confirmed the custom hook throws an error when context is null.</li>
</ul>

<hr/>

<h3>Cart Components (Part 4)</h3>
<p><strong>Generated by:</strong> GitHub Copilot</p>
<p><strong>Issues in AI output:</strong></p>
<ul>
  <li>Some import paths were incorrect; fixed to match my folder structure.</li>
  <li>Ensured all +/- buttons used <code>type="button"</code> (AI sometimes defaulted to <code>submit</code>).</li>
  <li>Added missing <code>aria-label</code> attributes for accessibility.</li>
  <li>Removed an unused “Proceed to Checkout” button that didn’t trigger anything.</li>
</ul>
<p><strong>Verified:</strong></p>
<ul>
  <li>Quantity selector clamps values using <code>Math.min</code> / <code>Math.max</code>.</li>
  <li><code>REMOVE_FROM_CART</code> and <code>UPDATE_QUANTITY</code> actions matched my CartAction type exactly.</li>
  <li>Cart badge updates correctly using <code>useCartContext</code>.</li>
</ul>

<hr/>

<h3>Checkout Form (Part 5)</h3>
<p><strong>Generated by:</strong> GitHub Copilot</p>
<p><strong>Concepts verified:</strong></p>
<ul>
  <li>Controlled components: every input uses <code>value</code> + <code>onChange</code>.</li>
  <li>Single <code>formData</code> object for all fields.</li>
  <li>Generic <code>handleChange</code> using <code>[name]: value</code> pattern.</li>
  <li><code>onBlur</code> validation with touched tracking via <code>Set&lt;string&gt;</code>.</li>
  <li>Inline errors using <code>aria-invalid</code>, <code>aria-describedby</code>, and <code>&lt;span role="alert"&gt;</code>.</li>
  <li>Submit-time validation before submission.</li>
  <li>Submission flow:
    <ul>
      <li><code>e.preventDefault()</code></li>
      <li>Show “Processing…”</li>
      <li>Dispatch <code>CLEAR_CART</code></li>
      <li>Show success message</li>
      <li>Disable button during submission</li>
    </ul>
  </li>
</ul>
<p><strong>Issues found & fixed:</strong></p>
<ul>
  <li>Ensured zip code validation uses a strict 5‑digit regex.</li>
  <li>Added null‑coalescing fixes in ProductCard for name/imageUrl.</li>
  <li>Converted all type imports to <code>import type</code> to satisfy <code>verbatimModuleSyntax</code>.</li>
</ul>

<hr/>

<h2>Session: Cart API Workshop</h2>
<hr/>

<h3>Part 1: Cart & CartItem Entities</h3>
<p><strong>Generated by:</strong> GitHub Copilot</p>
<p><strong>What AI produced:</strong></p>
<ul>
  <li><code>Cart.cs</code> with Id, UserId, CreatedAt, UpdatedAt, Items.</li>
  <li><code>CartItem.cs</code> with Id, CartId, ProductId, Quantity, navigation properties.</li>
  <li>DbContext updated with <code>DbSet&lt;Cart&gt;</code> and <code>DbSet&lt;CartItem&gt;</code>.</li>
</ul>
<p><strong>Reviewed against lecture patterns:</strong></p>
<ul>
  <li>Correct Cart → CartItem → Product relationship.</li>
  <li>Navigation properties present for Include/ThenInclude.</li>
  <li>UserId stored as string (hardcoded user pattern).</li>
  <li>No computed properties on entities.</li>
</ul>
<p><strong>Fixes I made:</strong></p>
<ul>
  <li>Ensured timestamps use <code>DateTime.UtcNow</code>.</li>
  <li>Verified foreign key types match Product and Cart primary keys.</li>
</ul>

<hr/>

<h3>Part 2: Request & Response DTOs</h3>
<p><strong>Generated by:</strong> GitHub Copilot</p>
<p><strong>Reviewed against lecture patterns:</strong></p>
<ul>
  <li>Request DTOs are thin: only client-sent fields, no computed values.</li>
  <li>Response DTOs include backend-computed totals (TotalItems, Subtotal, Total, LineTotal).</li>
  <li>Flattened properties: ProductName, Price, ImageUrl from Product navigation property.</li>
  <li>No foreign keys or entity leakage in responses.</li>
</ul>
<p><strong>Fixes I made:</strong></p>
<ul>
  <li>Ensured all computed fields are present on CartResponse and CartItemResponse.</li>
  <li>Made ImageUrl nullable to match Product model.</li>
</ul>

<hr/>

<h3>Part 3: FluentValidation</h3>
<p><strong>Generated by:</strong> GitHub Copilot</p>
<p><strong>Reviewed against lecture patterns:</strong></p>
<ul>
  <li>Validators extend <code>AbstractValidator&lt;T&gt;</code> with correct DTO types.</li>
  <li>Rules use <code>RuleFor(x =&gt; x.Property)</code> syntax.</li>
  <li>Custom <code>.WithMessage()</code> text for each rule.</li>
  <li>Quantity and ProductId rules match lab requirements.</li>
</ul>
<p><strong>Fixes I made:</strong></p>
<ul>
  <li>Installed <code>FluentValidation.AspNetCore</code> and registered validators in <code>Program.cs</code>.</li>
  <li>Confirmed validator classes are public and match DTO types exactly.</li>
</ul>

<hr/>

<h3>Part 4: CartController</h3>
<p><strong>Generated by:</strong> GitHub Copilot</p>
<p><strong>Reviewed against lecture patterns:</strong></p>
<ul>
  <li>Uses hardcoded <code>CurrentUserId = "default-user"</code>.</li>
  <li>GET uses <code>Include(c =&gt; c.Items).ThenInclude(i =&gt; i.Product)</code>.</li>
  <li>POST implements upsert: find-or-create Cart, then find-or-update CartItem by ProductId.</li>
  <li>GET returns 404 when no cart exists.</li>
  <li>Correct status codes: 200 (GET/PUT), 201 (POST), 204 (DELETE), 404 (not found).</li>
  <li>Uses response DTOs (CartResponse, CartItemResponse), not entities.</li>
  <li>Computed totals and LineTotal calculated on the backend.</li>
  <li>Flattened ProductName and Price from Product.</li>
  <li>Ownership checks on PUT/DELETE ensure CartItem belongs to CurrentUserId.</li>
</ul>
<p><strong>Fixes I made:</strong></p>
<ul>
  <li>Verified <code>.ThenInclude(i =&gt; i.Product)</code> was present so Product data is loaded.</li>
  <li>Ensured UpdatedAt is set whenever cart changes.</li>
  <li>Double-checked mapping for LineTotal and totals.</li>
</ul>

<hr/>

<h3>Part 5: Swagger Testing</h3>
<p><strong>Verified via Swagger:</strong></p>
<ul>
  <li>GET empty cart → 404 Not Found.</li>
  <li>POST add first item → 201 Created with correct CartItemResponse.</li>
  <li>GET cart → 200 OK with correct totals and item details.</li>
  <li>POST same product → quantity increments (no duplicate row).</li>
  <li>POST different product → second item added, totals updated.</li>
  <li>PUT update quantity → 200 OK with updated quantity and line total.</li>
  <li>DELETE one item → 204 No Content, cart updated.</li>
  <li>DELETE clear → 204 No Content, cart emptied or 404 on next GET (depending on implementation).</li>
  <li>POST invalid body (negative productId, quantity 0) → 400 Bad Request with FluentValidation errors.</li>
  <li>POST nonexistent productId → 404 Not Found.</li>
</ul>

<hr/>

<h2>Summary</h2>
<p>Across the Cart UI and Cart API labs, AI was used to generate reducers, context, React components, form logic, entities, DTOs, validators, and controller code. I reviewed all AI output against lecture patterns, fixed incorrect imports, adjusted action shapes, enforced validation rules, ensured correct Include/ThenInclude usage, and verified behavior through Swagger and UI testing. The final lab code matches the workshop checklists for both frontend and backend.</p>
